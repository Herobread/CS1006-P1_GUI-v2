package game;

public class Renderer {
	// TODO:

	// 1. store jframe (and other attributes) here

	// 2. initialise jframe in constructor

	// 3. create a method(decorator) to draw an image
	// so that in code it can just be this.drawImage("cave-d.png", 100, 200);

	// 4. create a method that gets CaveSystem and then based on that info
	// draws caves (use new getConnectionsString(Coordinates))
}
